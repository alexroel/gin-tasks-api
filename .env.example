# ========================================
# Gin Tasks API - Variables de Entorno
# ========================================
# Copia este archivo a .env y configura los valores

# ========================================
# Servidor
# ========================================

# Puerto en el que escuchará el servidor
PORT=8080

# Modo de Gin: debug, release, test
# - debug: muestra logs detallados (desarrollo)
# - release: optimizado para producción
# - test: para pruebas
GIN_MODE=debug

# ========================================
# Base de Datos (MySQL)
# ========================================

# Host del servidor MySQL
DB_HOST=localhost

# Puerto de MySQL
DB_PORT=3306

# Usuario de la base de datos
DB_USER=root

# Contraseña del usuario
DB_PASSWORD=tu_password_seguro

# Nombre de la base de datos
DB_NAME=gin_tasks_db

# ========================================
# JWT (JSON Web Token)
# ========================================

# Clave secreta para firmar tokens JWT
# IMPORTANTE: Usa una clave larga y aleatoria en producción
# Puedes generar una con: openssl rand -hex 32
JWT_SECRET=tu_clave_secreta_muy_segura_y_larga_minimo_32_caracteres

# Tiempo de expiración del token en horas
JWT_EXPIRE_IN=24

# ========================================
# Configuración Opcional
# ========================================

# Nivel de log: debug, info, warn, error
# LOG_LEVEL=info

# Habilitar CORS (orígenes permitidos)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate limiting (peticiones por minuto)
# RATE_LIMIT=100
